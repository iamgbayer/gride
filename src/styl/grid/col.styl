/**
 * col()
 * col($column, $align = (middle, top, bottom), $padding = $grid-column-gutter)
 */
col($column, $align = top, $padding = $grid-column-gutter)
  width $column * 100%
  padding 0 $padding
  display inline-block
  vertical-align $align
  font-size initial
