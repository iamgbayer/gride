/**
 * span()
 * span($column = 1/(number of columns), $align = (middle, top, bottom))
 */
span($column = 1/$grid-column, $align = top)
  $size = $column * 100%
  width $size
  display inline-block
  vertical-align $align
  font-size initial